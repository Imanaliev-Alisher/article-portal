@model IEnumerable<ContentPortal.Models.Article>
@{
    ViewData["Title"] = "Управление статьями";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>@ViewData["Title"]</h2>
    <a asp-controller="Articles" asp-action="Create" class="btn btn-primary">Создать статью</a>
</div>

<div class="row mb-3">
    <div class="col-md-6">
        <form asp-action="Articles" method="get">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Поиск статей..." value="@ViewData["CurrentFilter"]" />
                <button class="btn btn-outline-secondary" type="submit">Поиск</button>
                @if (ViewData["CurrentFilter"] != null)
                {
                    <a asp-action="Articles" class="btn btn-outline-danger">Сбросить</a>
                }
            </div>
        </form>
    </div>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show">
        @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Заголовок</th>
                        <th>Автор</th>
                        <th>Категория</th>
                        <th>Дата публикации</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var article in Model)
                    {
                        <tr>
                            <td>@article.Id</td>
                            <td>
                                @article.Title
                                @if (!string.IsNullOrEmpty(article.ImagePath))
                                {
                                    <span class="badge bg-info">📷</span>
                                }
                            </td>
                            <td>@(article.User?.FullName ?? article.User?.Email ?? "Аноним")</td>
                            <td>
                                @if (article.Category != null)
                                {
                                    <span class="badge bg-primary">@article.Category.Name</span>
                                }
                                else
                                {
                                    <span class="text-muted">Без категории</span>
                                }
                            </td>
                            <td>@article.PublishedAt.ToString("dd.MM.yyyy HH:mm")</td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a asp-controller="Articles" asp-action="Details" asp-route-id="@article.Id" 
                                       class="btn btn-sm btn-outline-primary" title="Просмотр">
                                        👁️
                                    </a>
                                    <a asp-controller="Articles" asp-action="Edit" asp-route-id="@article.Id" 
                                       class="btn btn-sm btn-outline-secondary" title="Редактировать">
                                        ✏️
                                    </a>
                                    <button type="button" class="btn btn-sm btn-outline-danger" 
                                            data-bs-toggle="modal" data-bs-target="#deleteModal@(article.Id)" title="Удалить">
                                        🗑️
                                    </button>
                                </div>

                                <!-- Modal -->
                                <div class="modal fade" id="deleteModal@(article.Id)" tabindex="-1">
                                    <div class="modal-dialog">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <h5 class="modal-title">Удалить статью?</h5>
                                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                            </div>
                                            <div class="modal-body">
                                                <p>Вы уверены, что хотите удалить статью "<strong>@article.Title</strong>"?</p>
                                                <p class="text-danger">Это действие нельзя отменить!</p>
                                            </div>
                                            <div class="modal-footer">
                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Отмена</button>
                                                <form asp-action="DeleteArticle" method="post" class="d-inline">
                                                    <input type="hidden" name="id" value="@article.Id" />
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit" class="btn btn-danger">Удалить</button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Index" class="btn btn-secondary">Назад к панели</a>
</div>
